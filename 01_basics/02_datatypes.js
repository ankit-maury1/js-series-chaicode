"use strict"; //Treat all code as newer version of JS.

// alert( 2 + 3 ) // we are using nodejs, not browser

console.log(3 + 65);
console.log("ankit");


let name="Ankit"
let age=20 
let isLoggedIn=false 
let state; //undefined
let temperature=null //null is a type of object but is undfined
// Primitive data types
//string data type
//number data type 2 power 53 range Use bigint for bigger numbers
//boolean => or boolean data type yeo or no true/false
// null => standalone alue
// Undefined => when value is not defined
// symbol => uniqueness

// Object

console.log(typeof (undefined)) 
console.log(typeof (name)) 
console.log(typeof age)
